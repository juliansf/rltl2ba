-- With trivial delay
{ a overlapped_until b }

or

-- With one letter delay
{ (a;b) overlapped_until (c;d) with delay r }

or

-- With possibly empty delay
{ (e;f) overlapped_until (g;h) with delay r* }

or

-- With complex delay
{ a overlapped_until b with delay (r;s* + t) }
