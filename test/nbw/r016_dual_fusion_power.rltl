-- With trivial delay
{ a overlapped_release b }

or

-- With one letter delay
{ (a;b) overlapped_release (c;d) with delay r }

or

-- With possibly empty delay
{ (e;f) overlapped_release (g;h) with delay r* }

or

-- With complex delay
{ a overlapped_release b with delay (r;s* + t) }
