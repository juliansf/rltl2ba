-- With trivial delay
{ a weak_until b }

or

-- With one letter delay
{ (a;b) weak_until (c;d) with delay r }

or

-- With possibly empty delay
{ (e;f) weak_until (g;h) with delay r* }

or

-- With complex delay
{ a weak_until b with delay (r;s* + t) }
