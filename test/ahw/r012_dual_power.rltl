-- With trivial delay
{ a release b }

or

-- With one letter delay
{ (a;b) release (c;d) with delay r }

or

-- With possibly empty delay
{ (e;f) release (g;h) with delay r* }

or

-- With complex delay
{ a release b with delay (r;s* + t) }
